server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # Arayuz ve API AYNI KOKENDEN servis ediliyor.
    #
    # Tarayici acisindan her sey http://localhost:3000 — bu yuzden
    # CORS diye bir problem YOK. Preflight istegi yok, Access-Control
    # basliklari yok, "neden calismiyor" yok. Ayri portlardan servis
    # edip CORS'la ugrasmaktansa tek kokende toplamak her zaman daha
    # basit.
    location /api/ {
        proxy_pass http://gateway:8080;
        proxy_set_header Host              $host;
        proxy_set_header X-Real-IP         $remote_addr;
        proxy_set_header X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 60s;
    }

    location /v1/ {
        proxy_pass http://gateway:8080;
        proxy_set_header Host $host;
        proxy_read_timeout 60s;
    }

    location /sink/ {
        proxy_pass http://gateway:8080;
        proxy_set_header Host $host;
        proxy_read_timeout 130s;   # /sink/slow 120 saniyeye kadar bekletebilir
    }

    location / {
        try_files $uri $uri/ /index.html;
        add_header Cache-Control "no-store";
    }
}
